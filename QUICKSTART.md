# 🚀 Быстрый старт

## Запуск через Docker (Рекомендуется)

```bash
# 1. Убедитесь, что вы в директории front
cd front

# 2. Запустите Docker Compose
docker-compose up

# 3. Откройте браузер
http://localhost:3000
```

## Локальный запуск

```bash
# 1. Установите зависимости
npm install

# 2. Запустите dev сервер
npm run dev

# 3. Откройте браузер
http://localhost:3000
```

## Сборка для продакшена

```bash
# Сборка
npm run build

# Предпросмотр
npm run preview
```

## Структура проекта

```
front/
├── src/
│   ├── components/      # Компоненты React
│   ├── hooks/          # Кастомные хуки
│   ├── App.jsx         # Главный компонент
│   ├── main.jsx        # Точка входа
│   └── index.css       # Глобальные стили
├── public/             # Статические файлы
├── Dockerfile          # Docker конфигурация
├── docker-compose.yml  # Docker Compose
└── package.json        # Зависимости
```

## Основные компоненты

- **Header** - Навигация с плавным скроллом
- **Hero** - Главная секция с анимациями
- **About** - О проекте GREENKG
- **Mission** - Миссия и цели
- **Structure** - Структура офиса
- **Projects** - Проекты и инициативы
- **News** - Новости
- **Events** - Мероприятия
- **Resources** - Образовательные ресурсы
- **Partners** - Партнёры
- **Contact** - Контакты
- **Footer** - Подвал сайта
- **ScrollToTop** - Кнопка прокрутки вверх
- **Loading** - Экран загрузки
- **ScrollAnimation** - Анимации при скролле

## Технологии

- React 18
- Vite
- Tailwind CSS
- React Icons
- Docker & Docker Compose

## Порты

- **Development:** 3000 (по умолчанию)
- **Docker:** 3000
- **Preview:** 4173

## Цветовая палитра

- **Primary (Синий):** #0056ea
- **Green:** #22c55e
- **Градиенты:** Синий → Зелёный

## Команды

```bash
npm install      # Установка зависимостей
npm run dev      # Dev сервер
npm run build    # Сборка для продакшена
npm run preview  # Предпросмотр сборки
```

## Docker команды

```bash
docker-compose up           # Запуск
docker-compose up -d        # Запуск в фоне
docker-compose down         # Остановка
docker-compose logs -f      # Просмотр логов
docker-compose restart      # Перезапуск
```

## Требования

- **Node.js:** 18+ (для локального запуска)
- **Docker:** Latest (для Docker запуска)
- **npm:** 9+

## Поддержка браузеров

- Chrome/Edge (последние 2 версии)
- Firefox (последние 2 версии)
- Safari (последние 2 версии)
- Mobile browsers

---

**Удачи в разработке!** 🚀
